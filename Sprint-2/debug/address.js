// Predict and explain first...
/*This is (address) an object that contains different data consisting of numbers 
and strings representing structured data with different properties (like houseNumber, name of street).  */
// This code should log out the houseNumber from the address object
// but it isn't working...
// Fix anything that isn't working

const address = {
  houseNumber: 42,
  street: "Imaginary Road",
  city: "Manchester",
  country: "England",
  postcode: "XYZ 123",
};

console.log(`My house number is ${address.houseNumber}`);
console.log(`My house is in ${address.street}`);
console.log(`My house is in ${address.city}`);
console.log(`My house is in ${address.country}`);
console.log(`My houses postcode is ${address.postcode}`);